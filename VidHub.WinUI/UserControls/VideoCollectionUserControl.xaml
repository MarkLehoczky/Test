<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="VidHub.WinUI.UserControls.VideoCollectionUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VidHub.WinUI.UserControls"
    xmlns:converter="using:VidHub.WinUI.Converters"
    xmlns:viewmodel="using:VidHub.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <converter:MissingPreviewConverter x:Key="EnsurePreview" />
        <converter:DateFormatConverter x:Key="FormatDate" />
        <converter:DurationFormatConverter x:Key="FormatDuration" />
    </UserControl.Resources>

    <UserControl.DataContext>
        <viewmodel:VideoCollectionViewModel />
    </UserControl.DataContext>

    <Grid AllowDrop="True" DragOver="DragOverItems" Drop="DropItems">
        <Image Source="/Assets/ClipboardImage.png" Visibility="{Binding IsEmpty}" MaxHeight="500" HorizontalAlignment="Center" VerticalAlignment="Center" />

        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Videos}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="10" MaxWidth="450">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Border Margin="0" Padding="0" BorderThickness="0" CornerRadius="10" HorizontalAlignment="Center" VerticalAlignment="Bottom" Grid.Row="0">
                                <Image Source="{Binding VideoPreview, Converter={StaticResource EnsurePreview}}" MaxHeight="425" Stretch="Uniform" />
                            </Border>
                            <TextBlock Text="{Binding Title}" FontWeight="Bold" TextTrimming="CharacterEllipsis" ToolTipService.ToolTip="{Binding Title}" Margin="10 5 10 0" Grid.Row="1" />
                            <TextBlock Text="{Binding Date, Converter={StaticResource FormatDate}}" FontStyle="Italic" Margin="10 0 0 10" HorizontalAlignment="Left" VerticalAlignment="Center" Grid.Row="2" />
                            <TextBlock Text="{Binding Duration, Converter={StaticResource FormatDuration}}" Margin="0 0 10 10" HorizontalAlignment="Right" VerticalAlignment="Center" Grid.Row="2" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
    
</UserControl>
